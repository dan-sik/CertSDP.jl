# Non-Diagonal Gram Certificate

Non-diagonal rank-one Gram matrix for `(x + y)^2`. The validation runner should
build an exact SOS Gram certificate and export a rational square decomposition
when the decomposition safety guard allows it.
