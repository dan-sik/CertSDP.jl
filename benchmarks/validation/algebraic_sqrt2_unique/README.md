# Irrational sqrt(2) feasible LMI

A full-suite algebraic benchmark whose isolated feasible point is `x = sqrt(2)`. It requires the optional `msolve` backend; the exact verifier still provides final acceptance.
