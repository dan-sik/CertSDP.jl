# workflow sumofsquares extracted sos

Workflow validation case representing a SumOfSquares-extracted SOS Gram problem. The runner imports the Gram JSON, performs exact coefficient matching, certifies, strict-verifies, and emits replayable artifacts.
