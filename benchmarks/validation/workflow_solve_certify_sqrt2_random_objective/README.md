# workflow solve certify sqrt2 random objective

End-to-end workflow fixture. The runner invokes the numerical oracle with random objectives, diagnoses the selected face, certifies exactly, and then gates the certificate through strict verification.
